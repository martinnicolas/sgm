<%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form-signin"}) do |f| %>
  <h2 class="form-signin-heading">Por favor regístrese</h2>
  <%= f.label :email, :id => "inputEmail", :class => "sr-only" %><br />
  <%= f.email_field :email, :id => "inputEmail", :placeholder => "Email", :class => "form-control", required: true, autofocus: true %>
  
  <%= f.label :password, :id => "inputPassword", :class => "sr-only" %><br />
  <%= f.password_field :password, :class => "form-control", :placeholder => "Clave", autocomplete: "off",required: true %>  

  <% if devise_mapping.rememberable? -%>
    <div class="checkbox">
      <label>
        <%= f.check_box :remember_me %> Recordarme
      </label>
    </div>
  <% end -%>

  <%= f.submit "Registrarme", :class => "btn btn-lg btn-primary btn-block" %>

  <br/>

  <%= render "devise/shared/links" %>  
<% end %>



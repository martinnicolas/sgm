<p id="notice"><%= notice %></p>

<div class="page-header"><h4>Listado de Secciones</h4></div>

<%= link_to 'Nueva Seccion', new_seccion_path %>

<br>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @seccions.each do |seccion| %>
      <tr>
        <td><%= seccion.nombre %></td>
        <td><%= link_to 'Ver', seccion %></td>
        <td><%= link_to 'Editar', edit_seccion_path(seccion) %></td>
        <td><%= link_to 'Eliminar', seccion, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>



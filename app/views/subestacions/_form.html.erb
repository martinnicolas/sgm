<%= form_for(@subestacion, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @subestacion.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@seccion_tomaestado_rutum.errors.count, "error") %> prohibited this seccion_tomaestado_rutum from being saved:</h4>

      <ul>
      <% @subestacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :codigo, "Código", class: "col-sm-1 control-label" %>
    <div class="col-sm-2">
      <%= f.text_field :codigo, class: "form-control", required: true %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :nombre, class: "col-sm-1 control-label" %>
    <div class="col-sm-4">
      <%= f.text_field :nombre, class: "form-control", required: true %>
    </div>
  </div>
  <%= f.submit :value => "Guardar", :class => "btn btn-primary"%>
<% end %>

<%= form_for(@subestacion) do |f| %>
  <% if @subestacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subestacion.errors.count, "error") %> prohibited this subestacion from being saved:</h2>

      <ul>
      <% @subestacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br>
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

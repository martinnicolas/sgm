<div id='map'></div>

<br/>

<%= form_for(@rutum, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @rutum.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@rutum.errors.count, "error") %> prohibited this rutum from being saved:</h4>
      <ul>
      <% @rutum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, class: "col-sm-1 control-label" %>
    <div class="col-sm-4">
      <%= f.text_field :nombre, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-1 col-sm-10">
      <%= f.submit value: "Guardar", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>

<script>
  $(document).on("ready", function() {
    L.mapbox.accessToken = 'pk.eyJ1IjoibWFydGluY2hvIiwiYSI6ImZlMTY1MzUwMjQ5MGQ3YjgyY2NkYTU2MzJiZWIyMWRiIn0.Yhwbui6KvzC0JziPkwWXuQ';
    var userMap = L.mapbox.map('map', 'martincho.nb477913')
    var featureLayer = L.mapbox.featureLayer().loadURL('http://localhost:3000/ruta/<%= params[:id] %>.json').addTo(userMap)
    console.log(featureLayer);    
    //getTrailPoints(userMap);

    /*Ajusto el mapa a los marcadores*/
    /*featureLayer.on('ready', function(){
      userMap.fitBounds(featureLayer.getBounds());
    });*/

    featureLayer.on('layeradd', function(e) {
      var marker, popupContent, properties;
      marker = e.layer;
      properties = marker.feature.properties;
      popupContent = '<div class="popup">' + 
                      '<h3><b>' + properties.name + '</b></h3>' + 
                      '<p>' + properties.address + '</p>' + 
                     '</div>';
      return marker.bindPopup(popupContent, {
        closeButton: false,
        minWidth: 200
      });
    });

    $('article li').click(function(e) {
      var current, currentlyClickedName;
      current = $(this);
      return currentlyClickedName = current.find('h2').text();
    });
  });
</script>